export default class Swagger {
    constructor(info) {
        this._info = info
    }

    swaggerToEditorSchema(swaggerSchema) {
        // swagger -> editor
    }

    editorToSwaggerSchema(editorSchema) {
        // editor -> swagger
    }

    // 拆分方法
    // …
}